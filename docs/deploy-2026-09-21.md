# Primeira instalação — 21/09/2026

O responsável afirmou nesta tarefa que revisou o projeto e autorizou correções e publicação. Aprovação registrada na conversa; não há alegação de revisão independente ou aprovação na API do GitHub.

## Escopo desta revisão

- Correção do contraste dos placeholders e do foco por teclado, com regressão automatizada.
- Provisionadores de **primeira instalação**, que recusam diretórios, contas, bancos e serviços preexistentes. Não usar como atualizador genérico.
- Ambientes separados, PostgreSQL com proprietário e runtime distintos, Redis autenticado próprio e ClamAV restrito ao loopback. Pacotes ClamAV são extraídos dentro do projeto sem instalação global nem execução de scripts de pacotes.
- Código em release identificada por SHA, dependências travadas e processos sem root.
- Virtual host exclusivo, certificado pelo webroot e staging HTTPS somente no loopback. Nenhuma alteração dos outros sites, certificados ou serviços.
- Banco novo recebe RLS antes de exposição pública; não há rollout sobre código antigo nem dados reais. A migration irreversível não será desfeita.

## Operação e limites

`infra/first_install.py` separa criação, antivírus, migrations, serviços e verificação. Deve ser executado por operações no servidor previamente inventariado. Os comandos exigem ambiente e SHA completo. O comando de migrations registra um dump verificável antes de aplicar. Credenciais são geradas no servidor e mantidas fora do checkout; diagnósticos completos de falha ficam em diretório privado.

`infra/first_install_tls.py` altera somente o novo vhost identificado por hash e marcador, faz backup antes de alterações, valida Nginx e faz reload gracioso. A publicação exige os registros de validação dos dois ambientes. O staging usa o mesmo domínio/certificado no proxy TLS, mas porta de loopback e aplicação, arquivos e credenciais separados.

Esta revisão não declara implantação concluída: CI, execução remota, restore, antivírus real, filas e HTTP devem produzir evidência. A criação de operador depende de identidade, campanha e permissões fornecidas pelo responsável. Não serão criadas contas demo em produção. Os 25 módulos continuam parciais conforme `implementation-status.md`; recuperação automática por e-mail, homologação visual autenticada e governança operacional ampliada não estão concluídas.

Referências dos parâmetros operacionais: [ClamAV](https://docs.clamav.net/manual/Usage/Configuration.html) e [Certbot webroot](https://eff-certbot.readthedocs.io/en/stable/using.html#webroot).

## Resultado executado — 14:22 UTC

Revisão instalada: `d42e740d01224f734e0bff1eb293350ee5a49934`.
[CI desta revisão aprovada](https://github.com/iamnothuman7/nabio-elege/actions/runs/35609796007).

- Staging isolado recebeu as migrations, 149 arquivos estáticos e 30 templates válidos. O papel de runtime passou na verificação das 73 tabelas com RLS forçado.
- O ensaio HTTP usou **105 requisições HTTPS**, incluindo login, CSRF negativo, troca obrigatória de senha, ativação de MFA, 44 listas e 42 formulários, criação sintética, isolamento e logout. A conta sintética foi desativada ao final.
- ClamAV real aprovou um PDF sintético e rejeitou o arquivo-padrão de teste EICAR. Upload/download privado passou; indisponibilidade simulada do scanner manteve o documento em quarentena e bloqueou o download.
- Uma tarefa administrativa percorreu Redis/Celery e retornou seu resultado em cada ambiente.
- Backup completo criptografado e restauração em **outro banco novo** passaram nos dois ambientes, incluindo hashes de arquivos/configurações, contagens, migrations e políticas RLS. Bancos e arquivos de ensaio foram preservados; somente arquivos temporários de empacotamento criados pelo próprio backup foram removidos após criptografia.
- Rollback de código compatível foi ensaiado em staging, com confirmação dos diretórios efetivamente carregados pelos workers e retorno à revisão candidata. Nenhuma migration foi revertida. Não representa ensaio de desastre ou rollback de schema incompatível.
- Certificado exclusivo de `elege.nabio.pro` emitido, válido até 20/12/2026, e simulação de renovação aprovada. Não foi ignorado erro de TLS. A primeira tentativa de renovação aguardou a rotina preexistente do servidor liberar seu lock; esse processo não foi interrompido.
- Produção foi criada com banco, segredos, Redis, antivírus, web, worker e scheduler próprios, todos os processos de aplicação sem root. Nenhuma conta/campanha demo foi inserida em produção.
- Sete rotas internas retornaram 200 e o formulário inexistente retornou 404, todos com cabeçalhos de segurança esperados. Verificações de dependências, worker, disco, backup e certificado passaram.
- Backup diário criptografado e monitor local a cada cinco minutos foram ativados. As falhas são registradas pelo systemd; encaminhamento de alertas e exportação periódica externa ainda exigem configuração operacional.
- Hashes dos outros vhosts e PIDs dos serviços de referência foram preservados. Apenas o novo vhost recebeu alterações e o Nginx foi recarregado graciosamente. Staging foi desligado/desabilitado depois da homologação para liberar memória, preservando seus arquivos e bancos.

### Bloqueio atual de abertura pública

**Acesso público não foi liberado.** A borda HTTPS permanece em HTTP 503 de manutenção, apesar de a aplicação interna estar operacional.

A revisão automática recusou, antes da execução, a exportação do backup e da chave de recuperação para uma pasta privada desta estação, por faltar autorização explícita para essa transferência de dados/credenciais. Nada foi exportado. Foi solicitada autorização ao responsável; não se contornou a recusa. A pasta de destino foi criada com ACL restrita e permanece sem essa cópia. A cópia externa prevista nas regras segue pendente.

Também permanecem pendentes os dados do responsável, campanha e permissões para criar o primeiro operador real. Não foram inventadas credenciais nem criadas contas demo em produção. A inspeção visual final do domínio após abertura e o fluxo real de primeiro acesso ainda não foram executados. Os limites funcionais em `implementation-status.md` continuam válidos.

Próximo passo: receber a autorização/destino da cópia externa, executar e verificar essa cópia; registrar a evidência privada; liberar exclusivamente o novo vhost; validar rotas, assets, HTTPS e apresentação no navegador. Não repetir a solicitação de revisão de código já feita pelo responsável. Não executar novo provisionamento `init` sobre os ambientes existentes.
